# Script for modifying ingress in all live branches

- I needed to modify ingress in every live branch for "cefizelj" and "omnia" repos **fast** to prevent cert issues.
- `modify_cefizelj_ingresses()` and `modify_omnia_ingresses()` mostly do the same things but in different directories.
